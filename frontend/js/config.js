// Punto unico para cambiar la URL del backend.
// Local: http://localhost:4000/api
// En despliegue, reemplaza por la URL publica de tu API (ej. Render).
window.API_BASE = window.API_BASE || 'http://localhost:4000/api';
